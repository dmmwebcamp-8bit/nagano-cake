<div class="container my-5">
  <div class="row">
    <div class="col-9 mx-auto">
      <h5 class="font-weight-bold mb-5"><span class="bg-light px-4">管理者ログイン</span></h5>

      <%= form_with model: @admin, url: admin_session_path do |f| %>
        <div class="form-group ml-5">
          <table class="table table-sm table-borderless">
              <td>メールアドレス</td>
              <td><%= f.email_field :email, placeholder: "sample@example.com", class: "form-control" %></td>
            </tr>
            <tr>
              <td>パスワード</td>
              <td><%= f.password_field :password, autocomplete: "new-password", class: "form-control" %></td>
            </tr>
            <tr>
              <td></td>
              <td><%= f.submit "ログイン", class: "btn  btn-primary font-weight-bold px-5 mt-4" %></td>
            </tr>
          </table>
        </div>
      <% end %>
    </div>
  </div>
</div>